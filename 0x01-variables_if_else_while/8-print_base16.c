#include <stdio.h>

/**
 * main - Entry point
 * Description: Print base 16 number
 * Return: Always 0 (success)
 */

int main(void)
{
	int a;
	char h;

	for (a = 0; a < 10; a++)
	{
		putchar(a + '0');
	}
	for (h = 'a'; h <= 'f'; h++)
	{
		putchar(c);
	}
	putchar('\n');

	return (0);
}
